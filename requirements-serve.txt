# Serving-only dependencies for ClermontAI (API + UI).
# Pinned to the exact versions in venv_new that are PROVEN to load the
# pickled artifacts in models/ and produce correct scores.
#
# NOTE: the artifacts in models/ were pickled under scikit-learn 1.8.0. Pinning
# to that exact version (not 1.9.0) avoids the InconsistentVersionWarning on
# every boot and removes any risk of attribute-layout drift between releases.

# --- core ML / numerics (must match the versions the .joblib files expect) ---
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.8.0
lightgbm==4.6.0
joblib==1.5.3
shap==0.52.0

# --- API layer ---
fastapi==0.137.0
uvicorn==0.49.0
pydantic==2.13.4

# --- UI layer ---
streamlit==1.58.0
altair==6.2.1
requests==2.34.2
